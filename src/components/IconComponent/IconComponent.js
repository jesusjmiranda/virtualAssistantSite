import styles from './IconComponent.module.css'
const IconComponent = (props) => {
    return (
        <div className={styles.IconComponent}>This will be the main ICON</div>
    )
}

export default IconComponent